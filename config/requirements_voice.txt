# Koto 语音模块依赖清单
# 使用: pip install -r requirements_voice.txt

# 核心语音识别
SpeechRecognition>=3.10.0

# 本地离线 STT - Vosk（中文模型 ~50MB，自动下载）
vosk>=0.3.45

# 音频处理 (PyAudio 可能需要特殊安装方式)
# PyAudio - 参考 install_voice_deps.ps1 获取安装说明

# 快捷键监听
keyboard>=0.13.5

# 数值计算
numpy>=1.24.0

# Web 框架
flask>=3.0.0
flask-cors>=4.0.0
flask-sock>=0.7.0

# 配置管理
python-dotenv>=1.0.0

# AI API
google-generativeai>=0.3.0

# 可选依赖 (增强功能)
# pyperclip>=1.8.2        # 剪贴板支持
# watchdog>=3.0.0         # 文件监控
# psutil>=5.9.0           # 系统信息
